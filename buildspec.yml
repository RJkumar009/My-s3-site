version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install"

  build:
    commands:
      - echo "Build phase - no build steps, just preparing files"

artifacts:
  files:
    - '**/*'
  base-directory: s3-site-build
