version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install"

  build:
    commands:
      - echo "Build phase started"
      - echo "Build completed successfully"

artifacts:
  files:
    - '**/*'
  base-directory: s3-site-build/
